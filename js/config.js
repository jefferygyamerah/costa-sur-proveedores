/**
 * Costa Sur Proveedores — Configuration
 * These are PUBLIC keys (anon key) — safe to commit.
 * Set up your Supabase project and paste the values here.
 */
var COSTASUR_CONFIG = {
  SUPABASE_URL: 'YOUR_SUPABASE_URL',
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY'
};
